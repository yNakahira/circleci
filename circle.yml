general:
  artifacts:
    - $CIRCLE_ARTIFACTS/eslint/report.html
machine:
  timezone: Asia/Tokyo
  node:
    version: 6.9.4
dependencies:
  post:
    - npm install -g gulp
test:
  override:
    - mkdir -p $CIRCLE_ARTIFACTS/eslint
#    - node_modules/eslint/bin/eslint.js --quiet --format=junit -o $CIRCLE_TEST_REPORTS/eslint/eslint.xml app
    - node_modules/eslint/bin/eslint.js -- -f node_modules/eslint-html-reporter/reporter.js -o $CIRCLE_ARTIFACTS/eslint/report.html
