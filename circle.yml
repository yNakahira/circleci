general:
  artifacts:
    - reports/report-results.html
machine:
  timezone: Asia/Tokyo
  node:
    version: 6.9.4
dependencies:
  post:
    - npm install -g gulp
test:
  override:
    - mkdir -p reports
#    - node_modules/eslint/bin/eslint.js --quiet --format=junit -o $CIRCLE_TEST_REPORTS/eslint/eslint.xml app
    - gulp lint --quiet -f node_modules/eslint-html-reporter/reporter.js -o reports/report-results.html $CIRCLE_ARTIFACTS
